import styles from './modal.module.css';

export const modalPosition = {
  top: styles.top,
  bottom: styles.bottom,
  center: styles.center,
};

export const modalSize = {
  small: styles.modal_small,
  medium: styles.modal_medium,
  large: styles.modal_large,
  extra_large: styles.modal_extra_large,
  full: styles.modal_full,
};
