import styles from './inputaddon.module.css';

export const inputAddonPositionStyle = {
  left: styles.left,
  right: styles.right,
};

export const sizes = {
  xs: styles.xs,
  sm: styles.sm,
  md: styles.md,
  lg: styles.lg,
};
