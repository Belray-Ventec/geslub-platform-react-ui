import styles from './select.module.css';

export const sizes = {
  xs: styles.xs,
  sm: styles.sm,
  md: styles.md,
  lg: styles.lg,
};

export const iconSizes = {
  xs: 13,
  sm: 17,
  md: 20,
  lg: 22,
};
